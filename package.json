{
  "name": "golf-oclock",
  "version": "1.5.1",
  "private": true,
  "type": "module",
  "description": "Personal cross-course tee time finder and alerter for the Merrimack Valley / southern NH area.",
  "engines": { "node": ">=20" },
  "scripts": {
    "poll": "node src/index.js",
    "poll:quiet": "node src/index.js --no-alerts",
    "test": "node test/logic.test.js"
  }
}
